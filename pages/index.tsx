import type {NextPage} from 'next'
import Head from 'next/head'

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Accessible Listbox</title>
        <meta name="description" content="Accessible listbox component" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <h1>Listbox</h1>
      </main>
    </div>
  )
}

export default Home
